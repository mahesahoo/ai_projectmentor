[pytest]
markers =
    live: exercises the real Gemini API - needs a working GEMINI_API_KEY, costs real quota, and is non-deterministic. Deselected by default; run explicitly with `pytest -m live`.
addopts = -m "not live"
pythonpath = .
